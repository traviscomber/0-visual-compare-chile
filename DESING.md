# VIDENTIA DESING MASTER LOCK

Status: ACTIVE — MUST FOLLOW

This filename is retained exactly as requested. The canonical, non-duplicated implementation contract is [`DESIGN.md`](./DESIGN.md).

Every visual change must follow `DESIGN.md` strictly. Do not create a second design specification here; if the design contract changes, update `DESIGN.md` so there is one source of truth.

Current active scope: **complete public landing** — Hero/Nav → Trust → How it works → Capabilities → Audiences → Portfolio/Watch/Deadlines → Final CTA → Footer.

The latest user instruction explicitly allows restrained atmospheric green/blue light, gradients and Bauhaus depth as documented in `DESIGN.md`. No additional concept images are required; implementation is code-first.
